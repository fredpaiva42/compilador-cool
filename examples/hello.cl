(* hello.cl — primeiro teste do analisador léxico.
   Este comentário de bloco ocupa duas linhas. *)

-- Comentário de linha: deve consumir até o fim da linha
class Main inherits IO {
    main() : Object { out_string("oi\n") };
};